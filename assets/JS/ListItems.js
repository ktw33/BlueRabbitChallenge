
const ListItems = (props) => {
  // const listItemsArray = []; 
  // for (let i = 0; i < namesArray.length; i++){
  //   listItemsArray.push(<li>{namesArray[i]}</li>)
  // }
  return (
    <li id="listItems">{props.name}</li>
  )
};

export default ListItems; 
